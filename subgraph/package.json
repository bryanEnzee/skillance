{
  "name": "skillance-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ skillance-subgraph",
    "create-local": "graph create --node http://localhost:8020/ skillance-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ skillance-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 skillance-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.37.0",
    "graphql-request": "^7.2.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.51.0",
    "matchstick-as": "0.6.0"
  }
}
